# Time is Precious — Agent Control Pack

Status: TEMPLATE — NOT ACTIVE

This folder stages governance rules for future coding-agent use. It is intentionally inactive for now.

Recommended activation order:
1. Resolve every repository-specific `TBD`.
2. Verify the current known-good game state.
3. Commit it and create a permanent checkpoint/tag.
4. Map protected paths and the authoritative game-design source.
5. Approve an isolated UI sandbox path.
6. Mark the control files `ACTIVE` only after Game Director approval.
7. Run the first pilot at `LEVEL 1 — ISOLATED / LOW-RISK`.
8. Increase agent permission only after reviewed successful runs.

Suggested first pilot after activation: an isolated Time is Precious UI sandbox with reusable components, with no production gameplay integration until human review.
